<section class="section">
  <div class="container">
    <div class="columns">
    <% if user_signed_in? %>
      <%= render 'profile' %>
    <% else %>
    
    <% end %>
    
      <%= render 'feed' %>
      <%= render 'trends' %>
    </div>
  </div>
</section>
