 <div class="">
	<article class="media">
	  <div class="media-content">
	    <div class="content">
	    <p>
	      <strong><%= comment.user.name %>: </strong>
	      <%= sanitize(Rinku.auto_link(comment.body)) %>
        </p>
	 </div>
	</div>
	        <% if user_signed_in? %>
				<% if current_user == @tweeet.user || current_user == comment.user %>
           		 <%= link_to 'Delete', [comment.tweeet, comment], method: :delete, data: { confirm: "Are you sure?" } %>
	        	<% end %>
			<% end %>
</article>
</div>